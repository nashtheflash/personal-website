import { MdxLayout,} from '@/app/components/blog'
import { generateMetadata } from '@/utils';

export const metadata = generateMetadata({
    title:"Tune M1 Insulation Pack Install",
    description:"How to install the factory insulation pack on the Tune M1 camper",
    keywords: ['Tune M1', 'Insulation Pack', 'Install', 'How to']
});


<MdxLayout>

## How to install tune insulation pack


</MdxLayout>
