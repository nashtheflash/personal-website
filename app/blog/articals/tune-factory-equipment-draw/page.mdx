
import { MdxLayout,} from '@/app/components/blog'
import { generateMetadata } from '@/utils';

export const metadata = generateMetadata({
    title:"Tune M1 Equipment Power Draw",
    description:"Power specs for the standard Tune M1 Equipment",
    keywords: ['Tune M1', 'Power Specs', 'Lights', 'Fan', 'Heater']
});


<MdxLayout>
    <h1>This is how much power is drawn by the: lights</h1>
    <h1>This is how much power is drawn by the: fan</h1>
    <h1>This is how much power is drawn by the: heater</h1>
</MdxLayout>
